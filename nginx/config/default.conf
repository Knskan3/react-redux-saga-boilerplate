server {
    listen 80;
    server_name  localhost;

    root /usr/share/nginx/html/;

    location / {
      root   /usr/share/nginx/html/;
      index  index.html index.htm;
    }

    location /test {
      return 200 'ok';
    }

}
